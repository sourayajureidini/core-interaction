// piece of code that does one or more actions

/*
 function go() {
 alert ('hi')
 alert('hey there')
}


// it didnt work first because we didnt name the function >

go();
go();


 go(); is the name of the function , we can name a function as many times as we like


function go(name, age) {
  alert ('name')
  alert('age')
}
// the term function is how you define a function
//go is the name of the function
// whats in the brackets is an argument
// arguments can be used many times > called: DRY CODE

go('Will',34);
go('Souraya',22);

// go (function name)+arguments

var name = prompt();

function add(first,second) {
return first+second;
}

 var sum = add(1,2);
 // sum is the name of the variable, add is the name of the function and (1,2) is whats in the function

 */
